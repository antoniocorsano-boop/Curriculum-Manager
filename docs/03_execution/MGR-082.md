# MGR-082 — COMPLETION_MAP_READONLY_UI

## Summary

Prima superficie UI read-only: mostra la mappa globale di completamento del Curriculum di Istituto.

La mappa dà visibilità a tutti gli utenti su aree, stati, cosa manca e cosa è pronto per validazione, senza introdurre autosave, presa in carico, subentro o documento laterale.

## Files changed

* `src/data/completionMapCatalog.js`
* `src/views/completionMapView.js`
* `src/app.js`
* `src/components/sidebar.js`
* `index.html`
* `src/styles.css`
* `docs/03_execution/MGR-082.md`
* `report/CONTROLLO_MGR082_COMPLETION_MAP_READONLY_UI.txt`
* `REPO-MOVELOG.md`

## Comportamento introdotto

* Voce sidebar "Mappa" accessibile a tutti.
* Vista `completionMap` con titolo "Mappa completamento curricolo".
* Riepilogo aree totali, in lavorazione, parziali, non avviate, da revisionare.
* Griglia aree: titolo, stato, descrizione, cosa c'è, cosa manca, evidenze, documenti collegati.
* Messaggio prudente: validazione umana/collegiale richiesta.
* Nessun salvataggio, nessun storage, nessun fetch.

## Confini

* Read-only: nessuna CTA di acquisizione, completamento, subentro.
* Nessun autosave, localStorage, sessionStorage, IndexedDB.
* Nessun profilo reale: la mappa è visibile a tutti senza filtro.
* Nessun documento laterale: solo riferimenti testuali.
* Nessun backend/API/OAuth/cloud/sync.
* Nessun DOCX/PDF programmatico.
* Dati statici di esempio, senza persone/studenti reali.
* Non dichiara conformità automatica.

## Validazioni

* `git diff --check`: passed.
* `node --check src/data/completionMapCatalog.js`: passed.
* `node --check src/views/completionMapView.js`: passed.
* `node --check src/app.js`: passed.
* `node --check src/components/sidebar.js`: passed.
* Scan pattern: nessun match non ammesso.
* Nessun dato personale/studente reale.
* Nessuna contaminazione `e86064b`.

## Cosa resta fuori

* Activity ownership e subentro (MGR-086).
* Profilo reale e filtri (MGR-078 + MGR-080).
* Workspace e documento laterale (MGR-083 + MGR-084).
* Autosalvataggio (MGR-085).
* Contesto Wiki avanzato (MGR-087).
* Evidenze e stati completi (MGR-088).

Expected post-merge verdict:

`MGR_082_CLOSED_MERGED_REMOTE`
