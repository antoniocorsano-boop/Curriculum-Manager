# MGR-109 — POST_FIX_DEMO_SMOKE_RUN

## Obiettivo

Verifica demo reale dopo `MGR-108B`, senza introdurre nuove funzioni e senza modificare runtime.

## Baseline

- Repo usato: `C:\Users\anton\Curriculum-Manager-MGR-108B`
- Branch: `feat/mgr-108b-demo-risk-runtime-fix-revision-document-buttons`
- Commit baseline: `70249af fix: clarify revision workflow and document actions`
- Server locale: `py -m http.server 8000`
- URL verificato: `http://127.0.0.1:8000/index.html`
- Browser di smoke: Chrome headless CDP `149.0.7827.115`
- Contesto:
  - `MGR-107`: baseline demo ready.
  - `MGR-108A`: rischi demo identificati.
  - `MGR-108B`: fix principali applicati su Revisione e Documenti.
- Vincoli rispettati:
  - Nessuna nuova feature.
  - Nessuna modifica runtime in `MGR-109`.
  - Nessun backend/cloud/API/auth/deploy.
  - Nessun `.env`, token, secret o file pesante.
  - Nessun dato reale: profilo browser temporaneo e `localStorage.clear()` prima dello smoke.

## Audit cartelle locali

- `git worktree list` ha mostrato molte cartelle slice.
- La cartella `Curriculum-Manager-DEMO` è risultata non adatta: HEAD `9ea07c3`, non contiene `70249af`.
- La cartella corretta per la demo è `Curriculum-Manager-MGR-108B`, che contiene `70249af` sul branch `feat/mgr-108b-demo-risk-runtime-fix-revision-document-buttons`.

## Percorso eseguito

1. Apertura app su `http://127.0.0.1:8000/index.html`.
2. Verifica caricamento iniziale, assenza di errori console/JS e copy local-only.
3. Navigazione verificata tramite sidebar renderizzata:
   - Materiali
   - Documenti
   - Output
   - Revisione
4. Revisione:
   - notice visibile;
   - textarea funzionante;
   - salvataggio annotazione locale verificato;
   - pulsanti `Prepara per il confronto`, `Reset annotazioni`, `Scarica copia dati`, `Scarica bozza testuale`, `Mostra percorso`;
   - nessun salto anomalo a inizio pagina sui controlli cliccati.
5. Documenti:
   - apertura dettaglio documento;
   - scrittura bozza locale;
   - salvataggio;
   - reload;
   - ripristino bozza;
   - nessun salto anomalo a inizio pagina, solo scroll atteso sul dettaglio documento.
6. Output Center:
   - apertura corretta;
   - pulsanti `Scarica JSON`, `Scarica Markdown`, `Reset tutti gli output`;
   - nessun riferimento visibile a cloud/backend/API/token/`.env`/segreti/deploy/rete.
7. Draft workflow:
   - bozza locale scritta in textarea;
   - salvataggio in `localStorage`;
   - reload e ripristino valore;
   - reset con conferma;
   - nessun dato reale usato.

## Esito per vista

| Vista | Esito | Note |
|-------|-------|------|
| Materiali | PASS | App caricata, sidebar visibile, copy local-only e validazione umana presenti, nessun errore console. |
| Documenti | PASS | Notice di bozza visibile, pulsante `Apri Centro output documenti` visibile, apertura dettaglio funzionante. |
| Output | PASS | Centro output aperto, export JSON/Markdown e reset tutti visibili, copy local-only chiaro. |
| Revisione | PASS | Notice bozza locale visibile, textarea funzionante, salvataggio localStorage verificato. |
| Mappa | DEMO_RISK | La sidebar renderizzata non espone `Mappa`; `index.html` e `app.js` hanno renderer/sezioni, ma `renderSidebar()` genera solo 4 voci. |
| Stati attività | DEMO_RISK | La sidebar renderizzata non espone `Stati attività`; stesso gap di navigazione. |
| Wiki | DEMO_RISK | La sidebar renderizzata non espone `Wiki`; stesso gap di navigazione. |

## Esito Revisione

- Notice visibile: `Nota: ogni annotazione è una bozza di lavoro fino alla validazione del gruppo.`
- Chiaro che si tratta di bozza locale: sì, tramite copy di bozza/validazione e assenza di approvazione reale.
- Chiaro che non è approvazione formale: sufficiente per smoke, ma copy esplicito `non approvazione formale` non presente nella notice di Revisione.
- Textarea funzionante: sì.
- Salvataggio annotazione: verificato con valore `Bozza locale MGR-109` e chiave `cmDraftNotes`.
- Scroll anomalo: no sui controlli testati.
- Pulsante `Mostra percorso`: funzionante, `delta scroll = 0`.

## Esito Documenti

- Pulsanti rinominati visibili nel dettaglio documento:
  - `Prepara per confronto`
  - `Annota per revisione`
  - `Apri revisione collegata`
  - `Aggiungi alle note`
- Nessun pulsante sembra approvare formalmente.
- Apertura documento: funzionante.
- Salvataggio bozza: funzionante.
- Ripristino bozza: funzionante dopo conferma.
- Scroll anomalo: no; apertura dettaglio produce scroll atteso verso il dettaglio (`delta = 541`).
- Nota UX minore: conferma reset riporta `Riepristinare la bozza iniziale?` con refuso.

## Esito Output Center

- Apertura corretta.
- Export JSON disponibile: pulsante `Scarica JSON`.
- Export Markdown disponibile: pulsante `Scarica Markdown`.
- Reset completo: pulsante `Reset tutti gli output`, con conferma esplicita.
- Nessun riferimento visibile a cloud/backend/API/token/`.env`/segreti/deploy/rete.
- Copy local-only chiaro: `Output locale: bozza locale, revisione umana richiesta, nessun invio automatico, nessuna sincronizzazione remota. Non equivale ad approvazione istituzionale.`

## Problemi trovati

| Classe | Problema | Dettaglio | Azione consigliata |
|--------|----------|-----------|--------------------|
| DEMO_RISK | Sidebar renderizzata incompleta rispetto al percorso smoke richiesto | `renderSidebar()` genera solo `Materiali`, `Documenti`, `Output`, `Revisione`; il percorso richiesto include anche `Mappa`, `Stati attività`, `Wiki`. | Slice separata readonly-only per riallineare sidebar e renderer senza nuove feature. |
| COPY_POLISH | Notice Revisione non esplicita il divieto di approvazione formale | Il copy chiarisce bozza/validazione, ma non usa la formula esplicita `non approvazione formale`. | Micro-copy slice separata se richiesto dal gate demo. |
| UX_POLISH | Refuso nella conferma reset documento | Conferma: `Riepristinare la bozza iniziale?`. | Correzione copy minima in slice separata, non eseguita in `MGR-109`. |

## Screenshot note

- Nessuno screenshot allegato.
- Smoke eseguito in browser headless con profilo temporaneo.
- Nessun dato reale inserito o salvato.

## Verdict finale

`MGR_109_DEMO_SMOKE_PASS_WITH_DEMO_RISK`

Motivazione: il percorso core post-fix `MGR-108B` passa su Apertura, Documenti, Revisione, Output e Draft workflow locale. Rimane un rischio demo su `Mappa`, `Stati attività` e `Wiki` perché non sono esposti dalla sidebar renderizzata, pur essendo presenti sezioni/renderer nell'app.

## Prossima slice consigliata

`MGR-110 — RESTORE_FULL_DEMO_NAVIGATION_READONLY`

Obiettivo consigliato: verificare se la demo deve includere le 7 voci di navigazione e, solo in caso affermativo, riallineare `renderSidebar()` alla shell completa senza introdurre nuove feature runtime.
